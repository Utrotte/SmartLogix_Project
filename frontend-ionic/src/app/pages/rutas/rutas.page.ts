import { Component } from '@angular/core'; import { IonicModule } from '@ionic/angular'; import { CommonModule } from '@angular/common'; import { FormsModule } from '@angular/forms'; import { forkJoin } from 'rxjs'; import { RutasService } from '../../services/rutas.service'; import { TransportistasService } from '../../services/transportistas.service';
@Component({selector:'app-rutas',standalone:true,imports:[IonicModule,CommonModule,FormsModule],templateUrl:'./rutas.page.html',styleUrls:['./rutas.page.scss']})
export class RutasPage{loading=false;error='';items:any[]=[];transportistas:any[]=[];mostrarFormulario=false;nueva:any={codigoRuta:'',nombre:'',origen:'',destino:'',distanciaKm:0,tiempoEstimado:'',paradas:1,estado:'PROGRAMADA',idTransportista:0};constructor(private svc:RutasService,private transportistasSvc:TransportistasService){}ngOnInit():void{this.load();}load():void{this.loading=true;forkJoin({rutas:this.svc.listarRutas(),transportistas:this.transportistasSvc.listarTransportistas()}).subscribe({next:r=>{this.items=r.rutas||[];this.transportistas=r.transportistas||[];if(!this.nueva.idTransportista&&this.transportistas[0])this.nueva.idTransportista=this.transportistas[0].idTransportista;this.loading=false},error:()=>{this.error='No se pudo cargar la planificación';this.loading=false}});}crear():void{this.svc.crear(this.nueva).subscribe(()=>{this.mostrarFormulario=false;this.nueva={codigoRuta:'',nombre:'',origen:'',destino:'',distanciaKm:0,tiempoEstimado:'',paradas:1,estado:'PROGRAMADA',idTransportista:this.transportistas[0]?.idTransportista};this.load();});}nombreTransportista(id:number):string{return this.transportistas.find(t=>Number(t.idTransportista)===Number(id))?.nombre||'Sin asignar';}}

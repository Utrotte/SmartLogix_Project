import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.smartlogix.app',
  appName: 'SmartLogix',
  webDir: 'www',
  android: {
    backgroundColor: '#061d35'
  }
};

export default config;
